
  # Duplicate HERO section in React

  This is a code bundle for Duplicate HERO section in React. The original project is available at https://www.figma.com/design/yNoUQl0R9ksxvYuVltbAJI/Duplicate-HERO-section-in-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  