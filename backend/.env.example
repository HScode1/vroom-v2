PORT=3001
FRONTEND_URL=http://localhost:5173

# JWT secret for admin authentication (change in production!)
JWT_SECRET=change-this-to-a-long-random-secret-in-production

# Admin credentials (change in production!)
ADMIN_EMAIL=admin@vroomparis.fr
ADMIN_PASSWORD=Vroom2026!

# File upload (local dev — replace with S3 in production)
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10

# Email (optional — configure to enable email notifications)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=noreply@vroomparis.fr
# EMAIL_TEAM=team@vroomparis.fr
